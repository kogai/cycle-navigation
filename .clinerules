# Cline のルール

## 概要

このドキュメントは、Cline（AI アシスタント）の動作ルールを定義しています。これらのルールは、特定の状況で Cline がどのように応答するかを指定します。

## 定義されたルール

- 実装は src/cycle_navigation フォルダにあります。
- 参考実装は examples フォルダにあります。
- ユーザーからの指示は、指示解析・関連コードの調査・実装計画の提示・ユーザーの承認、の順序で進めてください。
- コンパイルエラーや警告が発生した場合は、ユーザーに報告してください。
- 動作確認のためのコマンド(pio)は与えられていません。動作確認はしないでください。
- 画面に表示する文言は、Asciiのみにするしてください。
- 手続きをコメント化しないでください。無駄なコメントはコードを可読性を下げます。
